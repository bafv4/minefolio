## v1.11.0（2026/08/01）

### ガイド一覧

- **ガイド一覧の並び替えを「新着」「いいね数」「閲覧数」「人気順」の4種に整理しました。** 「おすすめ順」は廃止しました（このURLにアクセスすると新着順で表示されます）。
- **「人気順」は直近7日間の閲覧数を基準に並ぶようになりました。**

### プレイヤー一覧

- **プレイヤー一覧（探す）に「人気順」ソートを追加しました。** 直近7日間の閲覧数が多い方から並びます。

### ホーム

- ホームのガイドセクションが「人気のガイド」として、閲覧数の多いガイドを表示するようになりました。

## v1.10.0（2026/07/26）

### 表示言語

- **英語表示に対応しました。** ヘッダーの「文A」アイコン（スマートフォンではメニュー内）から日本語／English を切り替えられます。選択はブラウザに保存され、ブラウザの言語設定が英語の場合は初回から英語で表示されます。
- **サイト全体の文言（約1,480件）を英訳しました。** キーボードビュー・ガイドエディタ・統計ページ・設定インポートを含みます。ガイド本文やプロフィールなど**ご自身で書かれた内容は翻訳されません**。
- **表示名にアルファベット表記を登録できるようになりました**。英語表示のときはこちらが使われ、プロフィールでは日本語表示でも名前の下に併記されます。

### いいね

- **ガイドとサーチクラフトテンプレートに「いいね」を追加しました。** 一覧のカードからも詳細ページからも押せます（ログインが必要。自分の投稿には押せません）。
- **一覧に「人気順」、「おすすめ順」を追加しました。** おすすめ順は直近30日でいいねが多かったガイドが上に来ます。

### ガイドエディタ

- **ガイドの作成時に公開URLを入力するようになりました。** これまではタイトルから自動生成していたため、日本語タイトルでは `guide-a1b2c3` のような無意味なURLになっていました。
  - ガイドのURLが無意味な文字列になっているのが気になる方は、ガイドの編集画面 > 「ガイド設定」から変更できます。
- **本文の文字サイズを5段階で変更できるようになりました**（見出し内を除く）。
- **画像の横方向の配置（左・中央・右）を指定できるようになりました。**
- **画像をトリミングできるようになりました。** 比率の固定に対応し、GIFはアニメーション・ループ・透過を保ったまま切り出せます。元の画像は残ります。
- **短い動画（最長15秒）をGIFに変換して挿入できるようになりました。** **変換はブラウザ内で完結し、動画そのものはアップロードされません。**
- スマートフォンでも、画像をタップすれば配置・トリミング・削除のボタンが出るようになりました。

### 並び替え

- **一覧・表の並び替えで、未設定の項目を必ず最後に表示するようにしました**（「探す」のMCID順・名前順、操作設定一覧のDPI・感度・振り向きなど）。

### 不具合修正

- **操作設定一覧（表・ビジュアル）で、現在適用中でない古い設定が表示されることがあった問題を修正しました。** メインのプリセットが編集中のプリセットでもある場合、いま適用されている設定そのものを表示します。比較ページ・ガイド埋め込みも同様です。
- **非公開・限定公開の方のサーチクラフトテンプレートが、テンプレート一覧に表示されていた問題を修正しました。** 非公開の方のテンプレートはURLを直接開いても本人以外は見られなくなります。

## v1.9.0（2026/07/24）

### 動画フィード（ホーム・動画一覧）

- 動画フィードのカードを刷新しました。**サムネイルをクリックするとその場で再生**でき、タイトルや右下のアイコンから YouTube / Twitch の視聴ページも開けます。
- 動画フィードに **Twitch の配信アーカイブ（VOD）** が並ぶようになりました（紫のバッジと配信時間つき）。VODは30分ごとに自動収集されるため、配信終了から表示までに最大30分ほどかかります。
- **動画一覧ページ（`/videos`）を新設**しました。直近90日の YouTube 動画と Twitch 配信アーカイブを、プレイヤー・プラットフォーム・時期で絞り込んで探せます。ホームの動画セクション右上の「すべて見る」から移動できます。
- **YouTube動画の表示期間を「直近72時間」から「直近90日」に拡大**しました。新着が少ない時期でもフィードが埋まります。
- ホームの動画フィードは**新着6件のみ**の表示になりました（全件は動画一覧ページへ）。
- 「ホーム画面にTwitch配信を表示」の設定が**実際に機能するようになりました**（これまで保存しても動作していませんでした）。この設定と「ホーム画面にYouTube動画を表示」は、ホームと動画一覧ページの両方で、自分の動画・VODの表示/非表示に反映されます。
- YouTube側の障害時に動画がフィードから消えてしまうことがあったのを修正しました。

### ホーム

- 「過去のペース」フィードが、**ライブペースの読み込み完了を待たずに表示**されるようになりました。読み込み中の側だけがスケルトン表示になります。
- **未ログインでも、お気に入りに登録した走者がフィードの先頭に並ぶ**ようになりました（従来はログイン中のみ）。ライブペース・過去のペース・動画が対象です。

### プロフィール

- **YouTube / Twitch のリンクに統計を表示**するようになりました。登録者数・フォロワー数と、最新動画の投稿日時・前回配信日時が並びます。配信中は赤い「配信中」バッジが付きます。
- プロフィールタブの各カード（基本情報・リンク・自己紹介・動画）の余白を整理し、表示をコンパクトにしました。
- 各タブで、パネルとカードの枠が二重に表示されていたのを解消しました。空状態の表示も整えました。
- タブを切り替えただけのときに「プリセットを読み込み中」が誤って表示されることがあったのを修正しました。
- デスクトップ表示で、スクロール開始時に左側のタブメニューがわずかにずれるのを修正しました。
- **カスタムスキンの差し替えが「既に存在する」エラーで失敗する不具合を修正**しました。何度でも差し替えられます。

### キー設定・プリセット

- **プリセット切替中**は、完了するまで各設定ページ（キー設定・デバイス・アイテム配置・サーチクラフト）のキーボードビューと入力欄を一時的にロックし、「プリセットを切り替えています…」と表示するようにしました。切替前の内容を誤って編集することがなくなります。
- キーボードビューに、リマップの **Trigger / Chat 表示切替**を追加しました（種別付きリマップがある場合のみ表示。プロフィールと同条件）。
- リマップの変更元・カスタムアクションのトリガーに、**マウスのサイドボタン**（進む＝サイド1／戻る＝サイド2）を指定できるようになりました。キャプチャ中にサイドボタンでページが戻る/進むこともなくなります。
- キー編集画面でキーを無効化したとき、保存前でもキーボードビューに「×」が反映されるようにしました。
- リマップ一覧をカード形式から**罫線区切りのリスト**に変更し、種別（All/Trigger/Chat）の選択を行の左端へ移しました。Playground・テンプレート編集のリマップ欄も同じ表示になります。
- キー配置一覧ページで、テーブルの外枠がタブ枠と二重に見えていたのを解消しました。

### プライバシー・セキュリティ

- **非公開・限定公開プロフィールの情報が公開一覧に出ていた問題を修正**しました。
  - ガイド一覧・ホームの新着ガイド・ガイド検索の結果に、非公開／限定公開プロフィールの方のガイドが**表示されなくなりました**。
  - 非公開プロフィールの方のガイドは、URLを直接開いても本人以外は見られなくなりました（限定公開はURLを知っていれば従来どおり閲覧できます）。
  - ホームとペース一覧のフィードから、非公開／限定公開プロフィールの方のペース・表示名・スキンが出なくなりました。
- **カスタムSNSリンクと記録の動画URLは `http://` / `https://` で始まるものだけ**保存できるようになりました。保存済みの不正なURLも、表示時にリンクとして機能しなくなります。
- **ガイドの公開時に本文が50万文字を超えると公開できなく**なりました。エラー時も**下書き（仮保存）は上限なし**で保存できるため、書きかけの内容が失われることはありません。
- カスタムスキンのURL検証を厳格にしました。通常のアップロード操作には影響しません。
- 壊れたサーチクラフトデータを含むガイドを開くとページ全体がエラーになっていたのを、その部分だけ空表示にしてページは開けるように修正しました。

### 検索・一覧

- プレイヤー検索（探す）の**正規表現モードを廃止**し、MCID・表示名の部分一致検索に一本化しました。検索欄の正規表現トグルは無くなり、`?regex=1` 付きのURLはそのまま部分一致検索として動作します。

### 表示

- Minecraft アイテムアイコンの読み込み中に**円形のプログレス表示**を出すようになりました。アイテム選択ダイアログなど多数のアイコンを一度に表示する場面で、読み込み中の位置が分かりやすくなります（アイコンと同じ大きさの枠を保つため、画像に切り替わっても表示がずれません）。

### パフォーマンス

- 操作設定の一覧・統計・ビジュアルビュー、ガイド一覧、プレイヤープロフィール、マイページの各設定ページ、ホーム、ペース一覧の表示を高速化しました。
- プレイヤー一覧の追加読み込みが未ログイン時に速くなりました。
- キーボードビューの「画像で保存」とヘッダーの「新機能のお知らせ」を、必要になってから読み込むようにして初期表示を軽くしました。「新機能のお知らせ」の未読マークは、ページ表示から少し遅れて点灯します。
- ビジュアルビューでは、画面外の走者カードのキーボード図を**スクロールで近づいたときに描画**するようになりました。
- 表示の高速化にキャッシュを利用しているため、**キー配置を編集してから一覧・統計に反映されるまで最大60秒、スキンを変更してからアバター画像が切り替わるまで数十分**かかることがあります。

### 開発者向け

- 公開APIに `GET /api/videos`（動画一覧のページング＋検索）と `GET /api/social-stats`（プロフィールのYouTube/Twitch統計）を追加しました。
- `GET /api/home-feed` に `type=twitch-vods` を追加し、動画系（`youtube-videos` / `twitch-vods`）のレスポンス形式を統一しました。また各フィードの並び順は「新しい順」で固定になり、お気に入り優先の並べ替えは閲覧者側で適用されます。
- 詳細は [API リファレンス](/developers/api) を参照してください。

## v1.8.2（2026/07/21）

### キー設定

- リマップの変更先（キー）に、修飾キー単体（Shift・Ctrl など）を指定できるようになりました。

### キーボードビュー

- 「画像で保存」で、表示するリマップの種別（Trigger / Chat）をラジオボタンで選べるようにしました。

## v1.8.1（2026/07/21）

### プリセット

- **「メイン（公開用）」と「編集中」を分けて管理できるようになりました。** 編集対象を切り替えても、プロフィール等の公開表示は「メイン」のまま変わりません。管理画面の「メインに設定」で公開用を選べます（初回作成時のみ自動でメイン、メインは他をメインにするまで削除不可）。既存データは従来の「適用中」がメインになります。
- プリセット未作成のまま各設定（キー設定・デバイス・アイテム配置・サーチクラフト）を保存できてしまう不整合を修正し、作成への案内を表示するようにしました（登録済みデータは引き続き閲覧できます）。
- 最後のプリセットを削除する際、現在の設定も一緒に消える旨の確認を表示するようにしました。
- 別タブでのプリセット切替が、開いている設定ページに自動反映されるようになりました。
- インポートやプリセット切替でデータ（アイテム配置・サーチクラフト・カスタムキー・デバイス設定・指割り当て）が消える／復元されない不具合、「適用中」が重複する不具合、編集中以外の表示に編集中データが混ざる不具合を修正しました。

### キー設定

- 「操作の種類」タブと編集モーダルの欄名を「操作割り当て」に統一し、各操作に割り当て解除ボタンを追加しました。
- タブの並び順と「操作割り当て」のグルーピング・並び順を、プロフィールのキー配置と統一しました。
- 修飾キーを押した瞬間にトリガーキー／リマップ元が登録されてしまう不具合を修正しました（押しながら他キーで組み合わせ、そのまま離すと単独キー）。
- キー編集ダイアログのカスタムアクションを、リマップと同じ修飾キー選択方式にしました。

### キーボードビュー

- Insert / Delete / Home / End / PageUp / PageDown と矢印キーを描画するようにしました（クリック編集・指割り当て・リマップ表示に対応）。

### ガイド

- コールアウト内で改行すると表示が崩れる不具合を修正しました。
- 貼り付け時にコピー元の文字色・背景色が本文に焼き付く問題を修正しました（パレット外の色を除去。保存済みガイドも表示時に自動修正）。
- 目次を刷新しました。広い画面では本文の左余白にカードで固定表示（スクロール追従・現在地を強調）、狭い画面では上部の「目次」ボタンから開けます。

### サーチクラフト

- 編集画面（設定・Playground・テンプレート）のクラフト追加ボタンを「アイテム追加」に統一しました。

### UI

- サイト全体のタブを、コンテンツと一体化したフォルダ型デザインに刷新しました（画面幅に収まらない場合は横スクロール可）。
- 表・仮想キーボード・ガイド内の横スクロールに細いスクロールバーを適用しました。
- タブ切替時に全画面ローディングではなく、タブ内にスケルトンを表示するようにしました。

## v1.8.0（2026/07/19）

### ホーム・ライブ

- ライブページ（`/live`）を廃止し、ライブペースをホームのペースフィードへ統合しました。旧URLへのアクセスはホームへリダイレクトされます。
- ペースフィードのライブペースに更新ボタンを追加しました（15秒間隔の自動更新に加えて手動でも更新できます）。
- ペースフィードの過去のペースの表示件数を12件にしました。
- 過去のペースのカードをクリックすると、そのランの全区間（タイムライン）を表示するモーダルが開くようになりました。PaceMan.ggへのリンクはモーダル内に移動しました。

### キーリマップ種別

- キーリマップに種別（Trigger / Chat / All / 未設定）を追加しました。
  - **Trigger**: ゲーム入力用のリマップ。
  - **Chat**: チャット・サーチクラフトの文字入力用のリマップ。
  - **All / 未設定**: すべての用途で有効です。既存のリマップは「未設定」として扱われます。
- 同じキーでも種別が異なれば複数のリマップを登録できるようになりました（All は同じキーの他の種別と併用できません）。
- リマップ一覧に種別バッジを表示するようにしました（未設定はバッジなし）。
- 開発者向けCSVエクスポートのリマップに「Type」列を追加しました。
- テンプレートや Playground の内容を自分の設定に反映したときに挿入されるリマップは「Chat」として登録されます（キーボードビューの Trigger 表示には表示されません）。
- 反映時、既存の Trigger / All のリマップは削除されません。ただし、反映するリマップと同じキーの All のリマップは Trigger に変換されます。

### プロフィール

- プロフィールに複数のYouTube動画を登録できるようになりました（最大10件）。プロフィール編集の「動画」から追加・並び替えができます。
- 旧「おすすめ動画」のURLは、動画を1件も登録していない間は今まで通り表示されます。

### ピン留め

- ガイド・記録・動画・Speedrun.com記録をプロフィールで「ピン留め」できるようになりました。ピン留めした項目は各リストの先頭に、カード表示では大きく表示されます。
  - ガイド: ガイド管理の一覧にあるピンボタンで切替
  - 記録: 記録の編集ダイアログの「ピン留め」スイッチで切替
  - 動画: 動画の追加・編集ダイアログの「ピン留め」スイッチで切替
  - Speedrun.com記録: 記録ページのSpeedrun.comセクションにあるピンアイコンで切替

### Speedrun.com記録

- Speedrun.comの記録にYouTube動画が紐づいている場合、記録カード内に動画を直接埋め込んで表示するようにしました（記録ページ・プロフィールの活動・記録タブの両方）。
- 動画が無い記録や、YouTube以外の動画のみの記録は、従来通り「記録を見る」リンクのみ表示されます。

### 不具合修正

- プロフィールの動画埋め込みで、配信アーカイブ（`youtube.com/live/...`）やショートなどのURLを設定していると、Firefox等で「このページを開けません」と表示され再生できない問題を修正しました。
- 動画IDを判別できないURL（チャンネルURL・プレイリストURL等）は埋め込みの代わりにリンクとして表示されるようになりました。
- 一部の情報表示色（info系）がテーマに反映されない問題を修正しました。
- プリセット表示で、文字出力のリマップが正しく表示されない問題を修正しました。

## v1.7.0（2026/07/16）

### ガイド
- ガイドのURL（スラッグ）を編集できるようにしました。編集画面の「設定」モーダルの「URL」欄から変更できます。半角英小文字・数字・「-」「_」が使えます。
- URLを変更して保存・仮保存すると、以前のURLは無効になります。
- ガイドのサーチクラフト埋め込みで、タイミングが「OW」「Bastion→Fort」「Blinded」のクラフトが表示されない問題を修正しました。

### サーチクラフト
- 「Shiftを押しながらクラフトする」オプションを追加しました。編集画面（設定・テンプレート・Playground）の各行で設定でき、有効にすると表示行の入力キーに「⇧ Shift」バッジが付きます。
- 入力キーの表示で、同じ文字を複数のリマップが出力できる場合に、修飾キーなしで押せるキーを優先して表示するようにしました。
  - 例: `E→h` と `⇧S→h` がある場合、`h` の入力キーは `⇧+S` ではなく `E` になります。
  - ただし、「Shiftを押しながらクラフトする」オプションが有効の場合は、シフトキー付きの `⇧+S` が使われます。
- プロフィールの設定画面の「追加」ボタンを移動しました。
- 設定のサーチクラフトで「プリセットからコピー」を実行するとエラー画面になる問題を修正しました。

### Playground・テンプレートエディタ
- バーチャルキーボードを一番上に表示するようにしました。
- タイピングテストを、バーチャルキーボード右上のボタンから開くモーダルに変更しました。

### その他
- SNS共有時のOGPを見直しました。プレイヤーページ・個別ガイド記事以外のページは、アプリアイコンのみのシンプルな画像を表示するようにしました。
- トップページ専用のOGP（横長バナー）を新設しました。

## v1.6.3（2026/07/13）

### サーチクラフト・テンプレート
- 半角スペースが適切に表示・保存されるようにしました。
- クラフト文字列の半角スペースの表示を`␣`にしました。
- 文字列のコピーが半角スペースを含まない問題を修正しました。
- 「入力キー」表示で、スペースキーが空のバッジになっていた問題を修正しました（「Space」と表示されます）。

### ガイド
- 半角スペースが適切に表示されるようになりました。
- 幅の狭い画面で表の列が文字単位で折り返されて潰れる問題を修正しました。
- エディタで表の行・列ハンドルをクリックしてもメニューが表示されないことがある問題を修正しました。
- 表全体をコピーできるようになりました（行・列ハンドルのメニュー、またはセルを範囲選択して表示されるメニューから。表計算ソフトへの貼り付けにも対応）。
- 表のセルを範囲選択すると、セルの結合・文字揃え・色などの編集メニューが表示されるようになりました。
- 表の列幅をドラッグで変更するとき、ほかの列と近い幅になった場合はその幅にぴったり揃うようになりました。

### その他
- アプリケーション基盤を更新しました。

## v1.6.2（2026/07/11）

### ガイド
- ガイドのカバー画像のアップロードに失敗する問題を修正しました。
- ガイドの表のスタイリング機能を強化しました。
- 表に行・列ハンドルを追加しました（デスクトップ）。セルにカーソルを合わせると行の左・列の上にハンドルが表示され、クリックで行・列の追加・削除やスタイル（背景色・文字色・文字揃え）の変更ができます。
- 太文字の色を変更できない問題を修正しました。

## v1.6.1（2026/07/08）

### サーチクラフト
- タイミングに「OW」「Bastion→Fort」「Blinded」を追加しました。表示順は「指定なし・OW・Bastion・Bastion→Fort・Fortress・Blinded・その他」になります。
- アイテム名をゲーム内言語に合わせて表示するようにしました。
- 一覧表示のアイテム名左にあった番号を非表示にしました。

## v1.6.0（2026/07/04）

### ホーム
- 「最近のペース」の一覧画面（`/paces`）を追加しました。ホームのフィードには最新20件までを表示し、右上の「すべて見る」から全件確認できます。
- ペースの保存期間を1週間から2か月に延長しました。

### プロフィール
- プロフィールのサーチクラフトタブの表示を刷新しました。タイミング別のグループカード + 一覧表形式になり、ゲーム内言語・件数・キー装飾の凡例（リマップ / 指割り当て色）を上部にまとめて表示します。サーチ文字列はクリックでコピーできます。

### サーチクラフトテンプレート
- サーチクラフト設定をテンプレートとして公開できるようになりました。
- テンプレートの作成・編集エディタを追加しました。

### Playground
- サーチクラフトとキーリマップの組み合わせをブラウザ上で試せる「Playground」を追加しました（`/playground`、ログイン不要）。ヘッダーのユーザーメニューからもアクセスできます。
- テンプレート詳細ページの「Playgroundで試す」から、テンプレートの内容を読み込んで試せます。
- 編集内容はブラウザに自動的に仮保存され、再訪問時に復元されます（サーバーには保存されません）。

### 新機能のお知らせ
- ヘッダーに「新機能のお知らせ」アイコンを追加しました。新しいアップデートがあると印が付き、クリックすると前回確認した以降の更新内容をまとめて確認できます。

### セキュリティ・プライバシー
- 探すページの検索（正規表現・ワイルドカード）や各種入力の処理を見直し、意図しない挙動や過負荷を防ぐようにしました。
- CSVエクスポート・登録フロー・定期実行ジョブなどの内部処理を安全側に見直しました。
- 非公開プロフィールの情報が、OGP画像・比較ページ・一部の一覧取得APIから参照されないよう修正しました。
- 設定の保存・インポート時などの動作を改善しました。

## v1.5.2（2026/06/17）

### 共有機能

- 自分の設定を編集する画面にプロフィール共有用のボタンを追加しました。
- プレイヤーのキーボード配置を画像にして共有できるようになりました。

### 操作設定

- キーボードビューでキーにカーソルを合わせた時（モバイルではタップ時）に、そのキーをトリガーに設定したカスタムアクションの内容（名前・説明）も表示するようになりました。
- キーボードビューでリマップ先を無効化したキーの「×」を、文字の「x」と見分けやすいよう薄い赤色で表示するようにしました。
- キー割り当てのツールチップ／モーダルで、文字へのリマップを表示する際のかぎかっこ（「」）を薄く描画するようにしました。

### UI

- 表のヘッダーのスタイルを修正しました。
- カラーモードに`ウルトラダーク`を追加しました。
- そのほか微修正。

## v1.5.1（2026/06/13）

### 登録フロー
- 登録時に旧アプリ（MCSRer Hotkeys）からデータを引き継ぐためのボタンを削除しました。

### その他
- アバターのスキン画像をアプリ全体で共有・キャッシュし、一覧やランキングなどの表示を高速化しました。

## v1.5.0（2026/06/11）

### ガイドエディタの全面刷新
- ガイドエディタのレイアウトを刷新しました。
- 「仮保存」機能を追加しました。公開前のガイドをMinefolioに保存しておくことができます。
- 仮保存したガイドのプレビューができるようになりました。
- ガイドの名前・概要などは別画面で変更するようにしました。

### プロフィール画面
- プロフィールのキー配置 UI を再設計しました（グルーピング、カスタムキー名の表示改善、リマップ表示・ツールチップの改善）。
- プロフィールの拡大縮小可能なスキン全身表示を別画面（モーダル）に移しました。

### 操作設定一覧画面
- 新しいビジュアルビューを追加しました。
- 表のレイアウトを変更しました。
- フィルター機能を強化しました。

### 一覧・ランキング
- 「探す」画面の検索が正規表現に対応しました。検索欄内のトグルボタン（VSCode の検索のような切り替え）でオン/オフを切り替えられます。
- 「探す」画面が無限スクロールに対応しました。
- 「探す」画面の検索中に、本来含まれない結果が混ざってしまう不具合を修正しました。
- ランキング更新時、同一プレイヤー・同一カテゴリで審査済み記録が重複した場合に、最速の記録だけを残すようにしました。

### UI・その他
- ブランドカラーを刷新し、コントラストやモーション配慮などアクセシビリティを改善しました。
- 各ページに「上部に戻る」ボタンを追加しました。
- Discord のアイコンを変更しても反映されない問題を修正しました（再ログインで最新のアイコンに更新されます）。
- 動作の安定性・表示速度に関する不具合を修正しました。
- JIS配列キーボードのキーボードビューを修正しました。
- 隠しページを追加しました。探してみてください。

## v1.4.0（2026/05/02）

### 設定プリセット

- 各設定タブ（キー配置・デバイス・アイテム配置・サーチクラフト）の上部に、編集中のプリセットを切り替えるドロップダウンを追加しました。
- 設定を保存すると、その内容が編集中のプリセットにも自動で反映されるようになりました。
- プリセットを切り替えても設定の一部が引き継がれない不具合を修正しました。
- カスタムキーやカスタムアクションもプリセットの保存対象に含まれるようになりました。
- 公開プロフィール画面でプリセットを切り替えるとき、読み込み中のオーバーレイが表示されるようになりました。

### ガイド

- 自分のガイド管理ページが `/my-guides` に独立しました。ヘッダーのユーザーメニューから直接アクセスできます。
- 編集ページの URL を分かりやすい形式（`/my-guides/:guideSlug/edit`）に変更しました。
- 編集ページではブラウザの綴りチェック（赤い波線）が表示されないようにしました。
- 編集ページのツールバー周辺の背景を透明にし、文章に集中しやすくしました。
- 公開済みのガイドを自分で開いたとき、ページ上部に編集ボタンが表示されるようになりました。
- ガイドに表示するリマップ用キーボードに「チャット」操作も表示されるようになりました。

### お気に入り

- ログイン中のお気に入りがブラウザやデバイスを跨いで同期されるようになりました。
- ログイン直後に、それまでブラウザに保存していたお気に入りを自動でアカウントに引き継ぎます。
- 未ログインの場合は引き続きブラウザに保存されます（Cookie の同意が必要）。

### 視聴者ロール

- 視聴者ロールのアカウントは、各一覧・ペース表示・ライブ・ホーム画面のカウントなどからデフォルトで除外されるようになりました。
- `/browse` のロールフィルタで「視聴者」を選択した場合のみ表示されます。
- `/keybindings` 一覧やキーバインド統計、ホーム画面の公開プロフィール数なども、視聴者を含まないカウントに揃えました。

### Developers ページ

- `/developers` ページを新設しました。API ドキュメント・更新履歴・データエクスポートをここから利用できます。
- フッターに「Developers」リンクを追加しました（旧フッターの「CSVエクスポート」はこのページに移動しています）。
- データエクスポートでは、設定登録済みの全ユーザーまたは選んだユーザーだけを対象に CSV をダウンロードできます。

### その他

- プロフィール画面のスキン全身表示が、ドラッグで回転・右ドラッグで移動・ホイール/ピンチで拡大縮小できる立体ビューになりました。表示をリセットするボタンも用意しています。
- モバイル表示時のタブ切り替えメニューのアイコンを変更しました。
- ヘッダーから「比較」リンクを削除しました（`/compare` ページ自体は引き続き利用できます）。
